# TRASH WORLD NEWS

**地下计算机杂志**
> 第1期 1997年10月

**EXA**特别刊

## 目录
### 宣言
[***02*** **编者按:**《TRASH WORLD》宣言](#02-编者按trash-world宣言)

---

### 入门指南
[***05*** **使用 EXAs 进行分布式网络编程**](#05-使用-exas-进行分布式网络编程)  
[***08*** **Ghast带你闯关（教程）**](#08-ghast带你闯关教程)  
[***12*** **黑客技能:** 运行时错误及其利用方法](#12-黑客技能运行时错误及其利用方法)  
[***14*** **Axiom EXA 语言参考指南**](#14-axiom-exa-语言参考指南)  

---

### 黑客世界
[***18*** **调试噬菌体**](#18-调试噬菌体)  
[***22*** **硬件骇入:** 电子高速公路标志](#22-硬件骇入电子高速公路标志)  
[***24*** **网络探索:** 电子现金销售时点情报系统](#24-网络探索电子现金销售时点情报系统)  
[***26*** **访问传统存储系统中的数据**](#26-访问传统存储系统中的数据)  
[***28*** **网络探索:** Workhouse](#28-网络探索workhouse)  
[***30*** **网络探索:** Equity First 银行](#30-网络探索equity-first-银行)  

---

### 饿了么
[***32*** **TRASH WORLD 厨房**](#32-trash-world-厨房)  
[***34*** **似曾相识（上）**](#34-似曾相识-上)  

---

## *02* 编者按：《TRASH WORLD》宣言
&emsp;&emsp;欢迎阅读《TRASH WORLD NEWS》的首期。你或许会好奇，在万物皆数字化的今天，我们为何还要创办一本新纸质杂志。答案很简单：我们需要一个平台来分享关于计算机系统和网络的重要且敏感的信息——一个本身并不依赖计算机的平台。

&emsp;&emsp;你最近有没有注意到？无论你走到哪里，无论你做什么，总有一群计算机在幕后运作，对你做出各种判断。整个社会似乎都默认了一条“常态”：由当权者设计的计算机程序将决定我们在他们眼中的价值，并据此对待我们。

>我们需要一个平台来分享关于计算机系统和网络的重要且敏感的信息……

&emsp;&emsp;计算机本身只是执行预设程序的工具。正因如此，抗争的重任就落在了我们肩上。我们需要装备好工具和知识，才能抵御这股汹涌的浪潮——这样，每当某个庞大系统把我们当作待处理的穿孔卡片[^01]时，我们就能编写程序，向那些宏大设计[^02]的齿轮中投掷扳手，迫使它们承认我们作为人的真实存在。

### 我们是人，我们也是黑客。

&emsp;&emsp;本期将聚焦于小巧而强大的 EXA——这项在无人察觉的情况下悄然席卷全球的技术。如今，从掌上游戏机到大型企业服务器，几乎所有设备背后都离不开这些小巧的组件。

&emsp;&emsp;我们将带您详细了解这些内容的方方面面，希望您能快速上手，即使您之前从未接触过编程。

&emsp;&emsp;此外，我们还刊登了来自全世界各个领域的黑客的实用研究成果，内容涉及数据存储格式、值得关注的计算机网络等诸多方面。

&emsp;&emsp;最后，本期刊登了某位研究生物学家从一个非常有趣的角度解读了这种被称为“噬菌体”的疾病——她利用业余时间研究了这种疾病，并慷慨地与我们分享了她的笔记。我敢保证，这些信息在别处是找不到的。

---

### 黑客精神永存。
>Ghast留

## *05* 使用 EXAs 进行分布式网络编程
&emsp;&emsp;*要从计算机手中夺回自主权，第一步就是学会如何使用它们。而这意味着要掌握 EXA 编程。*

### 什么是EXA？
&emsp;&emsp;EXA 是“执行代理”（EXecution Agent）的缩写。这是一个小型程序，能够通过网络在计算机之间移动，且不会中断其正在进行的任务，即使跨越现实世界中巨大的距离也是如此。

&emsp;&emsp;本世纪初，Axiom 和 TEC 等大型科技公司基于他们所称的“分布式网络编程”愿景，开发了 EXA 标准。EXA 能够无视底层硬件的差异，自由传输和处理数据，从而轻松将世界万物连接在一起。如今，它们已成为所有现代计算机网络的命脉。

### 代码、值和寄存器
&emsp;&emsp;除了在计算机网络中以实体形式存在外，每个 EXA 还包含**代码区**和**寄存器**。如果您通过 EXODUS 开发环境连接到网络，您会看到左侧为每个 EXA 的窗口，该窗口允许您编辑其代码并查看其寄存器。

&emsp;&emsp;**代码区** 这是一组指令列表，用于告诉 EXA 该做什么。它使用一种专门为 EXA 设计的特殊计算机语言编写。我们将在接下来的教程部分深入探讨这种语言。

&emsp;&emsp;**寄存器** 不妨将它们视为能够存储值的“插槽”。这些值可以是**整型**，比如 `38`、`5074` 或 `-203`，也可以是字符串，比如 `SECRET`、`TRASH WORLD NEWS` 或 `ABC123`。寄存器可以通过代码中的指令进行读写操作。

&emsp;&emsp;寄存器有不同的类型。有些寄存器仅用于存储值，因此当你读取它们时，它们会返回你上次写入的值（这些是 **X** 和 **T** 寄存器）。其他寄存器实际上是更高级功能的接口，例如读写文件（**F** 寄存器）或与其他 EXA 通信（**M** 寄存器）。不必担心一开始就要掌握所有寄存器的用法。在后续教程中，你会逐步学习如何使用不同类型的寄存器。

## 主机、链接、文件和硬件寄存器
&emsp;&emsp;以下是一些您在使用 EXA 探索网络时将会看到的内容：

### 主机
&emsp;&emsp;主机是网络中的一个实体。EXA 只能存在于主机内部。当你创建一个新的 EXA 实例时，它们会位于你的主机中，但运行时，它们可以上传到目标主机并在那里执行。

&emsp;&emsp;由于系统性能和内存的限制，所有主机同时容纳的EXA数量均受限。在 EXODUS 中，这通过主机内部的可用网格空间数量来体现。每个 EXA 占用一个网格空间。

### 链接
&emsp;&emsp;链接将主机相互连接，EXA 正是通过这些链接在主机之间移动。链接的两端都有一个整形标识符，方便您在代码中引用它们。大多数链接支持双向通行，但有些仅限单向通行，还有些则完全无法使用。

### 文件
&emsp;&emsp;文件用于存储值的序列，包括整形和字符串。它们与 EXA 有些相似，因为它们存在于主机中并占用空间，但本身不包含代码或行为。它们的唯一用途就是存储值。

&emsp;&emsp;要操作文件，你需要使用 EXA。EXA 可以创建和删除文件，向文件写入和从文件读取数据，以及通过拖放操作移动文件。与链接类似，每个文件都有一个整形 ID，让你可以在代码中引用它。

### 硬件寄存器
&emsp;&emsp;某些主机包含特殊的硬件寄存器。这些寄存器与可能连接到主机计算机的外部硬件相关联。具体情况因系统而异，因此这些寄存器有时仅支持读取，有时仅支持写入，而有时则两者兼备。这些寄存器可能包含整形或字符串。这些寄存器的名称由井号（#）后跟四个字符组成，例如 `#POWR` 或 `#ENAB`。您可以在代码中直接使用这些名称。

---

&emsp;&emsp;信息量很大，所以没能一次全部掌握也没关系。下一节包含一些教程，你可以通过这些教程亲身体验该系统，并真正开始学习它。

## *08* Ghast带你闯关（教程）
&emsp;&emsp;我坚信任何人都能学会 EXA 编程。正因如此，我搭建了一个包含一些教程的资源库，供大家尝试。即使你觉得自己不是程序员，也请试一试。说不定你会给自己带来惊喜。

### 教程 1：基础知识
&emsp;&emsp;在本教程的第一部分中，你的目标是将ID为`200`的特定文件从名为“inbox”的主机移动到名为“outbox”的主机。你将通过ID为`800`的链接进行移动。

&emsp;&emsp;启动 EXODUS 并连接到教程网络。系统将自动为您创建一个新的空白解决方案。（“解决方案”只是 EXA 文件的集合，当您点击“运行”按钮时，这些文件便会开始执行。）

&emsp;&emsp;现在，屏幕左侧会显示一个空白的EXA窗口，其中代码窗口为空白。请在 EXA 中输入以下五行代码：

``` C++
LINK 800 //通过链接800进入网络
GRAB 200 //抓起文件200
LINK 800 //由链接800从“inbox”进入“outbox”
DROP     //放下被抓起的文件
HALT     //终止 EXA
```

&emsp;&emsp;按下 **step** 按钮，程序将逐条指令执行。当你感到无聊时，可以按下 **run** 按钮，观看程序自动运行。

&emsp;&emsp;确认程序运行符合预期后，您可以点击 **fast-forward** 按钮，让程序一直运行到结束。恭喜，你刚刚编写了第一个 EXA 程序。

### 教程 2：读写文件
&emsp;&emsp;好。接下来的任务与第一个非常相似，只是你需要读写该文件，而不是仅仅移动它。请阅读 EXODUS 中的要求以获取完整说明。现在就去读一读吧。

&emsp;&emsp;要读取或写入 EXA 持有的文件，我们必须使用 **F** 寄存器。在执行操作的过程中，我们还将使用 **X** 寄存器来存储中间值。请将以下代码输入到默认的空 EXA 中：

``` C++
LINK 800   //通过链接800进入网络
GRAB 200   //抓起文件200
COPY F X   //从 F 读取数据，并将该值写入 X
ADDI X F X //计算 X + F，并将结果写入 X
MULI X F X //计算 X × F，并将结果写入 X
SUBI X F X //计算 X - F，并将结果写入 X
COPY X F   //从 X 读取数据，并将该值写入 F
LINK 800   //由链接800从“inbox”进入“outbox”
DROP       //放下被抓起的文件
HALT       //终止 EXA
```

&emsp;&emsp;当您逐步执行程序并运行 `GRAB` 指令时，请注意，文件 `200` 的窗口会出现在当前正在显示该文件的 EXA 窗口下方。

&emsp;&emsp;此外，请注意文件窗口中的“文件光标”，它会高亮文件中的第一个值。当 EXA 从 **F** 寄存器读取数据时，它将读取文件光标所指向的值。同样地，向 **F** 寄存器写入数据会替换文件光标所指向的值。如果文件光标位于文件末尾，则会将新值追加到文件末尾，而非替换现有值。

&emsp;&emsp;还有一点。读写 **F** 寄存器会自动将文件光标移动到文件中的下一个值。有时这很方便，有时则不然。

### 教程 3：EXA 之间的通信
&emsp;&emsp;接下来的教程涉及一个只能通过单向链接访问的文件。 ‍负责获取该文件的 EXA 将被困住，因此你需要创建两个 EXA ，并让它们相互通信。

&emsp;&emsp;这次，我希望你能独立编写代码，不过这里有些提示：

* 您可以使用 `MAKE` 指令创建一个新文件。生成的文件默认是空的，并将自动由创建它的 EXA 抓起。
* EXA 可以使用 `WIPE` 指令删除其抓起的文件。
* 两个 EXA 可以通过 **M** 寄存器相互通信。如果一个 EXA 将一个值写入 **M** 寄存器，而另一个 EXA 从 **M** 寄存器读取该值，则该值将从写入方传输至读取方。
* 如果一个 EXA 在读取 **M** 寄存器时，没有其他 EXA 在向其写入数据，或者反之亦然，它会等待，直到另一个 EXA 接通通信的另一端。你不需要精确地控制时间。
* EXODUS 用户界面中有一个切换开关，可用于将 **M** 寄存器设置为全局（global）或本地（local）模式。在全局模式下，只要两台 EXA 之间存在链接路径，它们即可在网络的任何位置进行通信。在本地模式下，两台 EXA 只有在位于同一主机时才能进行通信。最后，处于全局模式的 EXA 无法与处于本地模式的 EXA 通信，即使它们位于同一主机中也是如此。

### 教程 4：循环与条件语句
&emsp;&emsp;你的最后一项任务是创建一个包含整形序列的文件。为此，你需要使用一种叫做“**循环**”的结构，它能让你反复执行几行代码，直到满足停止条件为止。以下是一些提示：

* 您可以使用 `TEST` 指令对寄存器和/或数值进行比较，例如检查 **X** 是否等于 `38`（`TEST X = 38`）。如果测试结果为真，则 **T** 寄存器被置为1。如果测试结果为假，则 **T** 寄存器被置为 `0`。
* 单看 `TEST` 指令本身并没有太大用处，但如果将其与条件跳转指令（`TJMP` 和 `FJMP`）结合使用，就可以根据测试结果跳转到程序的不同部分。当 **T** 寄存器被置为 `1` 时，`TJMP`（真时跳转）生效；当 **T** 寄存器被置为 `0` 时，`FJMP`（假时跳转）生效。明白其中的关联了吗？你需要在程序的其他位置使用 `MARK` 指令指定跳转目标。
* 下面是一个循环的示例。假设你想将数值 `9999` 写入文件 10 次。你可以像这样实现，该方法通过 **X** 寄存器来记录循环的迭代次数：

``` C++
COPY 0 X    //将 X 寄存器重置为 0
MARK LOOP   //定义一个名为 LOOP 的标签
COPY 9999 F //将 9999 写入抓起的文件
ADDI X 1 X  //将 X 增加 1
TEST X = 10 //判断 X 是否等于 10，并将结果写入 T
FJMP LOOP   //如果 X 尚未等于 10，则跳转到 LOOP
```

* 当 `TJMP` 和 `FJMP` 指令读取 **T** 寄存器中的值时，它们并不关心该值是来自 `TEST` 指令，还是由你自己存入的。实际上，你可以将任何数据存入 **T** 寄存器，只要你接受下次执行 `TEST` 指令时该值会被清空即可。

---

&emsp;&emsp;好了，你已经完成了所有教程。恭喜你。现在你可以称自己为一名真正的 EXA 程序员了。虽然还有很多东西需要学习，但基础知识你已经掌握了。

## *12* 黑客技能：运行时错误及其利用方法
&emsp;&emsp;有时 EXA 会尝试执行一些被禁止的操作。一旦发生这种情况，该 EXA 就会被主机自动终止。从功能上讲，这与执行一条 `HALT` 指令的效果相同。虽然大多数错误都平淡无奇且毫无用处，但其中有些却能为你所用。

### 我们先从一些没用的错误开始吧：
* **除以零**

&emsp;&emsp;在电脑上不能进行除以零的运算。这是数学上的某种原理。不必太在意其中的原因，总之别这么做就行了。

* **字符串数学运算**

&emsp;&emsp;无法对字符串执行加法、减法、乘法等运算。数学运算仅限于数值。

* **访问无效的 F 寄存器**

&emsp;&emsp;如果你的 EXA 在未抓起文件时读写 **F** 寄存器，将会报错并终止运行。请务必先抓起文件。

* **访问无效的硬件寄存器**

&emsp;&emsp;与上一个类似。在尝试读取或写入硬件寄存器之前，请确保 EXA 位于与该寄存器相同的主机上。

&emsp;&emsp;在编写代码的过程中，你可能会遇到很多这类错误，并会在编写过程中逐步修复它们。不过，现在让我们来谈谈更有趣的那种错误。

&emsp;&emsp;有时你并不了解所用主机的所有细节。链接可能会改变，文件可能会消失。有些关于运行环境的信息，只有通过尝试执行代码并可能遭遇失败才能了解。这看起来或许有些杂乱无章，但你又不是在某家大型软件公司工作，在那里这种“杂乱”会招致白眼（如果你确实身处这样的环境，那我只能说声抱歉，或许你该考虑改变一下生活了）。在这里，唯一重要的是把目标完成。

### 以下是一些更易于利用的错误：
* **无效文件访问**

&emsp;&emsp;假设你想查找某个特定文件。尝试查找该文件的 EXA 程序会终止运行，但这样你就能确定该文件并不存在。

* **无效链接访问**

&emsp;&emsp;假设你正在寻找一个特定的链接。尝试访问该链接的 EXA 会终止运行，但现在你已经知道那个链接不存在了……看出规律了吗？

---

&emsp;&emsp;说真的，别害怕让你的 EXA 程序报错、崩溃、挂掉——不管你怎么称呼它。它们在你眼里可能看起来很可爱，但请记住，它们存在的意义就是不断生成、运算，然后死去。你可以在程序中利用这一点。

>别害怕让你的 EXA 程序报错、崩溃、挂掉

## *14* Axiom EXA 语言参考指南
&emsp;&emsp;EXA 虚拟机（EXA-VM）允许多个执行代理（EXA）在由主机组成的共享网络中运行。在该网络内，EXA 可以被动态创建、销毁，并从一台主机转移到另一台主机。即使多个 EXA 位于同一台主机上，EXA-VM 也能确保所有 EXA 独立且同时运行。

&emsp;&emsp;EXA 程序由一系列指令组成。每条指令需要零个或多个操作数。每条指令所需的操作数均使用简短的缩写形式指定：

```
R    寄存器
R/N  寄存器，或介于 -9999 至 9999 之间的整形
L    由 MARK 伪指令定义的标签
```

### 寄存器
&emsp;&emsp;**X** 寄存器是一个通用存储寄存器，可以存储整形或字符串。

&emsp;&emsp;**T** 寄存器是一个通用存储寄存器，可以存储整形或字符串。它也是 `TEST` 指令的目标寄存器，并作为条件跳转（`TJMP` 和 `FJMP`）的判断条件。

&emsp;&emsp;**F** 寄存器允许 EXA 读取和写入已获取文件的内容。当 EXA 获取一个文件时，其“文件光标”将定位于文件中的第一个值。读取 **F** 寄存器将读取该值；写入 **F** 寄存器将覆盖该值。读取或写入 **F** 寄存器后，文件光标将自动向前移动。写入文件末尾时，系统会追加新值而非覆盖原有内容。

&emsp;&emsp;**M** 寄存器控制着 EXA 的消息传递功能。当一个 EXA 向 **M** 寄存器写入数据时，该值将被存储在该 EXA 的出站消息槽中，直到另一个 EXA 从 **M** 寄存器读取并接收该先前写入的值。整形和字符串均可通过这种方式传输。

&emsp;&emsp;如果一个 EXA 向 **M** 寄存器写入数据，它将暂停执行，直到另一个 EXA 读取该值。如果一个 EXA 从 **M** 寄存器读取数据，它将暂停执行，直到有可读取的值。如果两个或多个 EXA 试图同时从另一个 EXA 读取数据（反之亦然），其中一个会成功，但哪个会成功是无法预测的。

&emsp;&emsp;默认情况下，一个 EXA 可以与同一网络中的任何其他 EXA 进行通信。可以通过在 EXODUS 接口中切换全局/本地设置，或执行 `MODE` 指令，将通信范围限制为同一主机内的 EXA。处于全局模式的 EXA 无法与处于本地模式的 EXA 进行通信，即使它们位于同一主机内也是如此。

### 硬件寄存器
&emsp;&emsp;某些运行 EXA-VM 的主机可能通过硬件寄存器提供对连接硬件的访问。硬件寄存器的有效名称由井号（“#”）后跟四个字符组成，例如 `#POWR` 或 `#ENAB`。根据主机的配置不同，硬件寄存器可能是可读写、仅可读或仅可写的。

### 指令

#### 值的操作
`COPY R/N R`

&emsp;&emsp;将第一个操作数的值复制到第二个操作数中。

`ADDI R/N R/N R`

&emsp;&emsp;将第一个操作数的值与第二个操作数的值相加，并将结果存储在第三个操作数中。

&emsp;&emsp;`SUBI`（减法）、`MULI`（乘法）、`DIVI`（除法）和 `MODI`（取模）指令均采用相同的语法。

`SWIZ R/N R/N R`

&emsp;&emsp;将第一个操作数的值与第二个操作数的值进行交换（以第二个操作数的值作为交换掩码），并将结果存储在第三个操作数中。交换指令可用于重新排列和/或提取整型中的各位，如下所示：

|  输入  |  掩码  |  结果  |
| ----- | ----- | ----- |
| 6789  | 4321  | 6789  |
| 6789  | 1234  | 9876  |
| 6789  | 3333  | 7777  |
| 6789  | 1211  | 9899  |
| 6789  | -4321 | -6789 |
| -6789 | -4321 | 6789  |
| 6789  | 2000  | 8000  |
| 6789  | 0001  | 0009  |

#### 分支
`MARK L`

&emsp;&emsp;用指定的标签标记此行。`MARK` 是一个伪指令，不会被执行。

`JUMP L`

&emsp;&emsp;跳转到指定的标签。

`TJMP L`

&emsp;&emsp;如果 **T** 寄存器等于 `1`（或任何非 `0` 的值），则跳转到指定的标签。这对应于 `TEST` 结果为真。

`FJMP L`

&emsp;&emsp;如果 **T** 寄存器等于 `0`，则跳转到指定的标签。这对应于 `TEST` 结果为假。

#### 值比较
`TEST R/N = R/N`

&emsp;&emsp;将第一个操作数的值与第二个操作数的值进行比较。如果两者相等，则将 **T** 寄存器设为 `1`；否则，将 **T** 寄存器设为 `0`。`<`（小于）和 `>`（大于）比较也使用相同的语法。

|              | `TEST R/N = R/N` | `TEST R/N < R/N` | `TEST R/N > R/N` |
| ------------ | ---------------- | ---------------- | ---------------- |
| 整型/整型     | 测试相等性         | 测试整形顺序       | 测试整形顺序       |
| 字符串/字符串 | 测试相等性         | 测试字母排序       | 测试字母排序       |
| 整型/字符串   | 永假              | 永假              | 永假              |

#### 生命周期
`REPL L`

&emsp;&emsp;创建此 EXA 的副本，并跳转至副本中的指定标签。

&emsp;&emsp;如果某个 EXA 在执行 `REPL` 指令时正在持有文件，则该文件不会被复制，并将继续由原始 EXA 持有。

`HALT`

&emsp;&emsp;终止此 EXA。如果它正在持有文件，则该文件将被丢弃。

`KILL`

&emsp;&emsp;终止与该 EXA 位于同一主机上的另一个 EXA，优先处理由同一用户创建的 EXA。如果有多个可能的目标，目标将以不可预测的方式被选中。

#### 移动
`LINK R/N`

&emsp;&emsp;访问指定 ID 的链接

`HOST R`

&emsp;&emsp;将当前主机的名称复制到指定的寄存器中。

#### 通信
`MODE`

&emsp;&emsp;在全局和本地之间切换 **M** 寄存器模式。

`VOID M`

&emsp;&emsp;从 **M** 寄存器读取并丢弃一个值。

`TEST MRD`

&emsp;&emsp;如果该 EXA 能够在不暂停的情况下从另一个 EXA 读取数据，则将 **T** 寄存器设置为 `1`；否则，将 **T** 寄存器设置为 `0`。

#### 文件操作
`MAKE`

&emsp;&emsp;创建并抓起一个新文件。

`GRAB R/N`

&emsp;&emsp;抓起指定 ID 的文件。

`FILE R`

&emsp;&emsp;将当前文件的 ID 复制到指定的寄存器中。

`SEEK R/N`

&emsp;&emsp;将文件光标向前（正值）或向后（负值）移动指定数量的值。

&emsp;&emsp;如果 `SEEK` 操作会将文件光标移动到文件开头或结尾之外，则光标位置将被限制在文件边界内。因此，您可以使用 `-9999` 或 `9999` 作为参数，可靠地将光标移动到文件的开头或结尾。

`VOID F`

&emsp;&emsp;从当前持有的文件中移除文件光标所指向的值。

`DROP`

&emsp;&emsp;放下当前抓起的文件。

`WIPE`

&emsp;&emsp;删除当前抓起的文件。

`TEST EOF`

&emsp;&emsp;如果文件指针当前位于持有的文件末尾，则将 **T** 寄存器设置为 `1`；否则，将 **T** 寄存器设置为 `0`。

#### 杂项
`NOTE`

&emsp;&emsp;在编译时，`NOTE` 伪指令后面的任何文本都将被忽略，因此可以利用这一点来编写“注释”以说明代码。无论出现在行中的何处，分号后面的任何文本也将被忽略。

`NOOP`

&emsp;&emsp;在当前周期内不执行任何操作。

`RAND R/N R/N R`

&emsp;&emsp;生成一个介于第一个和第二个操作数之间（含两端）的随机数，并将结果存储在第三个操作数中。

## *18* 调试噬菌体
&emsp;&emsp;一种俗称“噬菌体”的神秘疾病，会将人体逐渐转化为计算机部件，这听起来像是科幻小说或阴谋论中的情节，但遗憾的是，现实中确实存在这种情况。在医学界，这种疾病被称为“进行性神经可塑性功能障碍”（这一名称已过时，且完全不准确），或较少使用的“Wurzner综合征”。

&emsp;&emsp;乍一看，变成一台电脑听起来似乎很酷——就像变成了赛博格[^03]一样。遗憾的是，这种噬菌体并不会将你的身体变成任何类似于正常运作的电脑的东西。如果任其发展，噬菌体最终只会让身体变成一堆毫无生气的、毫无用处的电路组合体。

&emsp;&emsp;我们认为，经常使用电脑的人更容易患上这种病，已知的所有病例都发生在每天使用电脑八小时或更长时间的人群中。这种病会在六到十八个月内从四肢（通常是双手，但偶尔也出现在双脚）向身体中心扩散，逐渐将人体细胞替换为一种含有半导体亚结构的塑料状物质所构成的连通结构。

&emsp;&emsp;目前治疗该噬菌体的最佳方案是，一旦出现病变组织就立即切除，但这显然并非理想的解决方案。目前还有其他几种疗法正在研发中。有一种超适应症口服药物似乎能有效延缓其扩散，但对大多数潜在患者而言，其价格高得令人望而却步。

&emsp;&emsp;除此之外，人们对这种噬菌体的了解仍然十分有限，因此各种猜测便争相填补这一空白。关于该噬菌体是某秘密政府实验室在研究人机接口时意外逃逸的实验体的说法无法得到证实；同样无法证实的是那些坦率地说相当荒谬的设想，例如某种神秘的“计算机大灾难”，据称碳基生命体与硅基生命体将融合在一起，形成一种更高层次的意识形态。

&emsp;&emsp;有一点我们可以通过实验验证，那就是噬菌体确实最初是被设计来作为一种促进人机直接交互的手段。作为一所知名医学院的研究生，我得以利用实验室与一位患有该病的熟人一起进行了一些“非正式”的实验。出于显而易见的原因，我们两人都将保持匿名。

### 将噬菌体节点看作 EXA 主机进行交互

&emsp;&emsp;关于噬菌体感染的组织，最有趣的一点或许在于，它似乎确实提供了可以连接到现成硬件调试器的接口。在本文所述的实验中，我使用了通常用于调试基于 EXA 的嵌入式系统的 Mitsuzen HDI-10。

&emsp;&emsp;通过将 HDI-10 的探针替换为导电的高精度镊子，成功连接到了下图显微镜图像中所示的噬菌体节点集群。每个噬菌体节点都被视为一个小型主机，且相连的噬菌体节点之间存在相应的链接。在缺乏正确链接 ID 的情况下，HDI-10 的固件似乎会自动为其进行编号。

&emsp;&emsp;已知噬菌体节点对神经（及类神经）细胞具有亲和力，会干扰神经活动，并引发与该疾病相关的退行性症状。进一步支持噬菌体旨在支持人机交互这一观点的是，连接至神经细胞的噬菌体节点在其对应宿主中呈现硬件寄存器，这些寄存器允许对连接神经细胞的电位进行测量、激发和抑制。单位似乎以毫伏为单位；对于不了解相关知识的人来说，神经的静息电位约为 `-70` mV，兴奋时可飙升至约 `50` mV，抑制时则可降至低至 `-120` mV。

### 将 EXA 注入噬菌体节点？

&emsp;&emsp;鉴于这项初步研究，似乎可以将EXA注入人类宿主体内的噬菌体结节中，并编程使其修复因这些噬菌体结节生长而受损的神经通路。我打算接下来尝试这一方法，但这显然是一项高风险的操作，且在医学界并不被认可（因此需要保密）。遗憾的是，像这样一次性、定制化的疗法恐怕难以获得监管批准。我打算尝试实施这一方案，并将后续汇报更多信息。

## *22* 硬件骇入：电子高速公路标志
&emsp;&emsp;我知道，对恶作剧电子高速公路指示牌怀有美好回忆的，不止我一个。控制板上的锁很容易撬开，而且很多时候根本就没上锁，所以以前把那些千篇一律、枯燥乏味的“**前方施工**”或“**左车道封闭**”之类的标语，改成更有趣的内容，比如“**无限速**”或“**撒旦[^04]万岁**”，简直轻而易举。

&emsp;&emsp;唉，很长一段时间以来，人们都以为这种事再也做不到了，这都要归功于当地交通部门——他们看穿了我们这项有益身心的活动，正将旧的高速公路路牌更换为一种昂贵的新型联网版本，这种路牌只能由总部远程编程控制。既然更改路牌的唯一方式是远程操作，孩子们肯定再也无法篡改它们了。对吧？

&emsp;&emsp;孩子们，你们都知道这个故事的套路：这些新型远程高速公路标志牌虽然骇入起来稍微有点麻烦，但要将其更改为任何你想要的内容依然很简单。你只需要阅读下面的教程和正确的编码，你也能让那位再次被困在车流中的公司工薪族露出笑容。

### 修改信息
&emsp;&emsp;进入系统后，这些显示屏接受以三组整形包形式写入 `#DATA` 的消息。第一组整形表示行号（从 `0` 开始），第二组表示列号（同样从 `0` 开始），第三组则对应你要显示的字符。此外，通常还有几个其他寄存器，玩起来很有趣。我见过的多数电子看板都有一个名为 `#CLRS` 的寄存器，向其写入任何内容都会清空显示屏。其他电子看板还具备可激活的特效，例如文字闪烁或反白显示。

&emsp;&emsp;以下是一个常见的字符表：

```C++
0 -    5 - E  10 - J  15 - O  20 - T  25 - Y  30 - 3  35 - 8
1 - A  6 - F  11 - K  16 - P  21 - U  26 - Z  31 - 4  36 - 9
2 - B  7 - G  12 - L  17 - Q  22 - V  27 - 0  32 - 5  37 - .
3 - C  8 - H  13 - M  18 - R  23 - T  28 - 1  33 - 6  38 - ?
4 - D  9 - I  14 - N  19 - S  24 - X  29 - 2  34 - 7  39 - !
```

### 实践
&emsp;&emsp;下图记录了一块高速公路路牌，可能被“某人”篡改了。毋庸置疑，无论是谁干了这件恶行，都该感到羞愧，而且这与我毫无关系[^05]……

>ISLANDMIKEY WUZ HERE

## *24* 网络探索：电子现金销售时点情报系统
&emsp;&emsp;如今，无论大企业还是小公司，都在争先恐后地将所有交易转为数字交易。为什么？因为这样更好，就是这么简单！别质疑了，顺应这股热潮，兄弟！！当然，在他们急于将一切数字化时，他们草草建好了所有系统，导致这些系统几乎漏洞百出，简直可笑。在这篇文章中，我将仅以一个例子为例，那就是“电子现金 POS”（全称是“销售时点情报系统”……若与另一个首字母缩写词[^06]存在相似之处，而该缩写词可能暗示该产品的质量，那纯属巧合）。

&emsp;&emsp;电子现金是支付处理领域中广受欢迎的选择，尤其受到中小企业的青睐。无论我在哪里查看，几乎都能看到它的身影……便利店、加油站、自助洗衣店，甚至美黑床。而且你可能不知道，所有由电子现金提供的系统都拥有共同的技术架构！

&emsp;&emsp;**客户数据库**（通常为文件 `200`）存储了一份客户列表，其中每个客户包含三个值：客户 ID、应付美元金额和应付美分金额。

&emsp;&emsp;**事务日志**（通常，正如你所料，就是文件 `201`）存储了一份事务列表，每条事务包含四个字段：日期、客户 ID、支付的美元数以及支付的分数。

&emsp;&emsp;你可能还会看到一个实时时钟模块，它通常连接到主主机上。该模块用于为交易添加时间戳。有时客户端会进行一些微调，可能是像自定义网络地图图形这样细微的改动，也可能是更重大的调整。例如我公寓楼下的一家自助洗衣店，就将其与他们的洗衣机和烘干机进行了集成。

&emsp;&emsp;总之，其核心架构始终如一。鉴于此， 从理论上讲，只要恶意攻击者能够访问其中一个系统，似乎就可以相当轻松地添加或删除交易记录或余额……

>…在他们急于将一切数字化时，他们草草建好了所有系统，导致这些系统几乎漏洞百出，简直可笑…

## *26* 访问传统存储系统中的数据
&emsp;&emsp;计算机技术发展日新月异，导致许多东西被淘汰，而向基于 EXA 的计算的快速转型，使得大量有趣的数据仍保存在旧式的存储介质上。访问 EXA 时代之前的存储数据可能有些棘手，但这也可能带来意想不到的收获。你永远不知道会发现什么！以下是几种较旧的存储格式，以及如何从它们中读取数据的方法：

### 磁带存储系统
&emsp;&emsp;磁带存储系统通常用于长期存储或备份。一个可被 EXA 访问的磁带存储系统通常由每台磁带机对应一台主机组成，每台磁带机上的磁带均挂载为可读写的文件。

&emsp;&emsp;每卷磁带包含零个或多个备份条目。这些条目的数据从磁带开头开始，并按顺序拼接在一起，因此无法分辨哪些数据属于哪个备份条目。哎呀！要检索这些数据可得费一番功夫。

&emsp;&emsp;条目的元数据以三元组的形式存储在文件末尾。第一个值是条目的名称，第二个值是数据流中条目数据开始的位置（`0` 表示开头），第三个值是条目的长度。

&emsp;&emsp;例如，要获取元数据为“`SECRET`, `3`, `4`”的条目数据，你需要从文件开头开始，跳过前三个值，然后按顺序读取接下来的四个值（按顺序）。

>1234, 5678, 9012, **3456**, **7890**, **1234**, **5678**, 0, 0, BORING, 0, 3, SECRET, 3, 4

### 磁盘矩阵
&emsp;&emsp;如今，硬盘矩阵仍在某些应用场景中被使用。由于它们将文件数据分散存储在多个硬盘上，因此读写速度更快，但访问起来也比单个硬盘更为复杂。在矩阵中，每块硬盘通常映射到其各自的主机，并包含最多 10 个文件，文件 ID 范围从 `200` 到 `209`。每个文件包含 100 个值，分为 10 个数据块，每个数据块包含 10 个值。

&emsp;&emsp;第一个驱动器中的第一个文件（`200`）应包含一个包含该驱动器阵列中所有备份条目的表。每个条目由条目名称及其后存储该条目数据的块地址组成。块地址是一个介于 `100` 到 `999` 之间的整形，指向驱动器阵列中某处的块。  首位整形表示包含该数据块的驱动器（`1`-`9`），第二位整形表示包含该数据块的文件（`0`-`9`，对应文件 `200`- 
`209`），末位整形表示该数据块在目标文件中的位置（`0`-`9`，对应偏移量 `0` 至 `90`）。

&emsp;&emsp;例如，要读取第 `527` 个数据块，你需要访问第 `5` 个驱动器，获取第 `202` 个文件，定位到偏移量 `70`，然后读取接下来的 10 个值（即一个数据块的大小）。嘿，其实也没那么复杂嘛！

```C++
CHUNK 527
DRIVE 5
FILE 202
OFFSET 70
```
## *28* 网络探索：Workhouse
&emsp;&emsp;如果你曾经需要一些急需的现金来维持生计，你可能考虑过 WorkHouse。广告把它描绘成赚外快的绝佳选择，但实际上，这些任务的乐趣大概就和看油漆干透一样。试想一下，手动在庞大的数据库中查找重复条目，或是盯着猪的模糊照片试图判断它们是否还能食用，又或是把某个富有的高管用公司公款享用的奢华晚餐收据输入电脑，这些工作能带来多少“乐趣”。

&emsp;&emsp;看到 WorkHouse 提供的这种诚实可靠的劳动，可能会让你不太愿意去实际完成任务，而更想一探究竟，看看这根“香肠”究竟是如何制作的。幸运的是，如果你能连接到服务器，其运作机制便会迅速展现在你面前，尽显其机械般的精妙。

&emsp;&emsp;**客户数据库** 首先你会看到一个名为“users”的主机，其中包含每个账户对应的文件。该文件记录了用户的姓名、出生日期，以及该用户每天的收入记录。WorkHouse 自然不希望有人欺诈他们，这倒也不难理解，所以他们大概将此作为追踪系统的一部分，以确保他们的“人畜[^07]”——哎呀，我是说独立承包商——按预期行事。

&emsp;&emsp;**工作数据库** 此外还有一个名为“jobs”的主机，其中包含关于当前可供用户的有效工作，以及这些工作的具体描述。

&emsp;&emsp;**任务数据库** “但任务都存储在哪里呢？”我听到你这么问。问得好。任务实际上是由发布任务的公司的合作伙伴网络提供的，并直接发送给工作者。这大概是为了简化流程……或者，也许是为了让 WorkHouse 面对系统中出现的任何可疑任务时，能有合理的否认余地。

&emsp;&emsp;无论如何，幸好大家都喜欢 WorkHouse ——这家公司每天都通过支付微薄的报酬，让人们从事那些重复且令人心力交瘁的工作，从而“帮助”人们提升自我。要是他们出了什么事，那可就糟透了！

>…如果你能连接到服务器，其运作机制便会迅速展现在你面前，尽显其机械般的精妙…

## *30* 网络探索：Equity First 银行
&emsp;&emsp;骇入银行？哇，这是什么，某种犯罪指南吗？当然不是……我们在这里都遵守法律，非常感谢！所以别以为你读完这篇文章，登录 Equity First 银行，就能突然往账户里多转进 10 万美元。你们都足够聪明，知道一笔如此巨额的交易会拉响警报，而且极有可能被欺诈检测程序发现。或者被计算机安全专家发现。

&emsp;&emsp;既然我已经明确了你的预期，我们就开始深入探讨吧。首先需要了解的是，尽管 Equity First 是那些大家“又爱又恨”的大型银行中规模最大的，但我们完全没有必要感到畏惧。我曾有机会一窥他们的网络内部，发现他们的内部服务器架构与其他银行并没有太大区别。事实上，它们相当简单。

&emsp;&emsp;**账户文件** 账户信息存储在单独的文件中，每个账户对应一个文件。账户文件以四个值开头。格式如下：账户号码、账户名称、一个表示账户类型的整形（到目前为止我发现 `2` 表示活期账户，`7` 表示贷款账户……其余整形想必是用于其他类型的账户），然后总是会有一个 `*`，随便它代表什么。这可能是为将来使用预留的占位符。

&emsp;&emsp;随后，账户中会填入四组数据，代表各项交易：交易资金的来源、去向账户，根据资金是流入还是流出账户，分别标记为 `CREDIT` 或 `DEBIT`；以及交易金额的具体数额（美元和美分）。Equity 会首先自动检查，确保每笔贷方交易都有对应的借方交易，以防止明显的做假账行为。

```C++
C20374, LINDA WALLACE, 2, *,
C72820, DEBIT, 20, 74,
C04272, CREDIT, 400, 00,
C04872, DEBIT, 33, 50,
```

&emsp;&emsp;从账户 `C20374`（`LINDA WALLACE`）向账户 `C04872`（`FASHION ALLEY`）转账 `33.50` 美元

```C++
C04872, FASHION ALLEY, 2, *,
C75293, CREDIT, 41, 33,
C82732, CREDIT, 74, 81,
C20374, CREDIT, 33, 50,
```

&emsp;&emsp;**ATM** 在网络后端，你会看到一些通过拨号连接接入的自动取款机。Equity First 的 ATM 仅提供 20 美元纸币，因此 `#CASH` 寄存器显示的是剩余纸币张数，而非剩余金额。若向 `#DISP` 寄存器写入 20，系统将尝试吐出一张纸币，但若 ATM 已无纸币，则会发送失败消息通知银行。请务必小心！

>…Equity First 是那些大家“又爱又恨”的大型银行中规模最大的…

## *32* TRASH WORLD 厨房
**你是个黑客；**

**你生活在网络之中。**

**物理现实并不重要。**

**你是一个精神构造，一种存在，**

**一个没有容器的灵魂……**

---
&emsp;&emsp;好吧，也许那是你的理想。但眼下我有些坏消息：你依然身处肉身之中！这意味着你得喂养它、照顾它等等。你懂的。过了一段时间，这些事就变得乏味，你也就不再去想这些了。 
 
&emsp;&emsp;也许你在这方面的生活体验，从在“最后一站”买些零食和汽水开始，也以同样的方式结束。如果真是这样，别担心。本专栏要讲的绝不是那种需要大把钞票、顶级食材或专业厨房设备才能完成的烹饪。那些东西太无聊了。况且，在当今世界，这样做总觉得不太对劲。我们需要一种烹饪方式，既能做出美味的食物，又能完全适应我们如今所处的这个……嗯，无论叫什么的“后”时代社会。

>*我们先从简单的开始。下面介绍……*

# 垃圾桶甜甜圈

&emsp;&emsp;没错，你没看错。你要把那些卖甜甜圈的店每天扔掉的、明明还很新鲜的甜甜圈“解放”出来。或者，如果你实在无法接受吃垃圾桶里东西[^08]的想法，那就趁它们因为放了一两天而打折时买下来。无论哪种方式，这些甜甜圈都还很好吃——可能只是稍微有点干硬罢了。这里有个诀窍：**炙烤档**。

&emsp;&emsp;什么东西？炙烤档！这是烤箱的一项功能，接下来你将学会如何使用它。通常烤箱会试图让内部受热均匀，但切换到炙烤模式后，顶部的加热元件就会启动，向正下方的食物释放出惊人的热量。你用它来让食物顶部或边缘变得酥脆，这是最后的点睛之笔。正是这一步让鸡皮变得酥脆，让奶酪融化起泡，让三文鱼排的边缘微微焦香。**烹饪真棒**！

&emsp;&emsp;之所以这样做有效，是因为烤箱的上火能将甜甜圈的外层烤得焦脆（这让口感更佳），同时还能将表面的糖霜或糖衣焦糖化（这让它变得酥脆且格外美味）。你也可以用烤面包机来做，不过它可能无法像烤箱上火那样将糖霜焦糖化。切勿使用下压式烤面包机……糖霜会滴落并烧焦，导致家中充满浓烟。甚至可能引发火灾。

&emsp;&emsp;现在，你可以完全免费（或花很少的钱）享用美味的热腾腾甜甜圈了！用烤箱还能做出一大堆美味佳肴，不过这些我们稍后再聊。现在，先好好享受你的升级版甜甜圈吧。

---

* 翻出你的垃圾桶（或打折的）甜甜圈 
* 用微波炉稍微加热一下，直到内部变软（一个甜甜圈约需 10 秒） 
* 将它们放在金属托盘或锡箔纸上
* 打开烤箱的炙烤档 
* 将甜甜圈放入烤箱，并调整烤架位置，使甜甜圈位于顶部的加热元件下方约 2 英寸处 
* 保持烤箱门敞开 
* 密切观察，因为顶部的加热元件升温迅速且温度很高 
* 待甜甜圈开始变色但尚未烧焦时取出（约 1 - 2 分钟）

## *34* 似曾相识 上
&emsp;&emsp;*编者按：是时候来点不一样的内容了。这是一部虚构作品，讲述了一个被科技改变的世界。这还不是我们所处的现实世界，但却是一个可能成为现实的未来……*
 
&emsp;&emsp;*请尽情享受这个故事，并期待下期刊载的结局。*

---
&emsp;&emsp;当我第一次得知自己是试管婴儿[^09]时，我脑海中浮现出这样一幅画面：一排排宽大的玻璃罐里孕育着胎儿，它们在昏暗的灯光下，成群结队地排列在实验室里。我的母亲莎伦正向我讲解关于细胞和线粒体的知识，而我的年轻保姆玛丽亚则轻抚着我湿漉漉的头发，把它梳理好并扎进发圈里。莎伦俯身在早餐吧台上，在一张散落的索引卡上画了一个形状模糊的圆点。

&emsp;&emsp;“这就是细胞，所有生命的基本单位。”

&emsp;&emsp;那时我六岁。那一年，是玛丽亚在我们家工作的最后一年。

---

&emsp;&emsp;什么才算是一位母亲？是怀胎十月吗？仅仅是分娩这一行为就定义了母性吗？还是说，养育和呵护的过程才真正定义了母性？

&emsp;&emsp;莎伦从来不需要男人，但这并非因为她是激进的女权主义者，也并非因为她极度独立。这只是她从未想过的事。她不需要任何人来生孩子。

&emsp;&emsp;莎伦曾是鲍威尔科学研究所的一名前途无量的研究员，曾在罗马尼亚、乌克兰以及美国各地工作，但经过15年的徒劳努力和屡屡破裂的合作关系后，她对高管层的厌恶促使她转向了一个更世故的领域——管理。于是，她在实验室里迅速适应了这一角色，沿着公司阶梯一步步向上攀爬，同时不断斩断身后的台阶。

&emsp;&emsp;“她为什么这么冷酷无情？”追悼会前几天，我问本。他停顿了一下，把那杯我冲得难喝透顶的咖啡放在了同一个早餐吧台上。他耸了耸肩。

---

&emsp;&emsp;我很想念玛丽亚，想念她给我编辫子时，一边唱歌一边用尤克里里伴奏的样子。她有一头卷发，脸上有酒窝，而且很容易晒黑。而莎伦和我则有着同样苍白的皮肤，光是想到太阳，皮肤就会起水泡。

&emsp;&emsp;玛丽亚曾抱着我，让我坐在她腿上，一边看老卡通片一边轻轻摇晃。她会喂我吃花生酱饼干，还会帮我梳头。她的头发又浓密又长，垂到腰际以下，她总是把它编成一条紧实的辫子。她是一条美人鱼吗？我至少问过她一次。她只是微笑着，咯咯地笑了起来。

&emsp;&emsp;“玛丽亚要去哪里？”

&emsp;&emsp;“玛丽亚正在组建自己的家庭。”

&emsp;&emsp;一股苦涩感瞬间涌上心头。 随后，迷茫的雾气笼罩了我的双眼。

&emsp;&emsp;“组建自己的家庭？”

&emsp;&emsp;“玛丽亚认识了一个不错的年轻人，要结婚了。”

&emsp;&emsp;“那你的年轻人呢？”

&emsp;&emsp;莎伦冲我咧嘴一笑，双手搭在我的肩膀上。 

&emsp;&emsp;“该开始学习了。”

---

&emsp;&emsp;玛丽亚走后没多久，莎伦就带回了一台亮紫色的全功能电脑。她说这是鲍威尔研究所人工智能部门送的礼物。当我打开电脑时，莎伦笑逐颜开，蹲在我身旁——那一刻，对我来说，简直就像是此生最接近圣诞早晨的时刻。电脑发出清脆的提示音，一道如浪潮般翻涌的波纹在反光屏幕上翻腾旋转。

&emsp;&emsp;“你好，” 设备里传来一个声音。“我叫乔迪。你叫什么名字？”

&emsp;&emsp;我盯着屏幕，看着我们两人的倒影。

&emsp;&emsp;“谢谢，” 我说。 

&emsp;&emsp;“你好，淘气鬼，”乔迪说道。

&emsp;&emsp;“虽然还有一些问题，但 AI 团队一直在进行修复。前景非常可观。我觉得你会非常喜欢拥有这样一个虚拟……朋友的。”

&emsp;&emsp;在接下来的三年里，我从未纠正过乔迪，而她一直叫我“淘气鬼”。

---

&emsp;&emsp;莎伦一直想做对我和她自己最有利的事。所以，我是由另一个女人代孕的。罗莎琳。

&emsp;&emsp;我第一次听到她的名字，是在偷听莎伦和本的谈话时。我顿时为之倾倒。我用一笔流畅的草书“**R**”[^10]在空中描摹她的名字，想象着她的面容，尽管我知道那与我的长相截然不同，也与莎伦的截然不同。

&emsp;&emsp;罗莎琳。罗莎琳。我曾想象她是一位有着一头长长的赤褐色长发和一双绿眼睛的公主。我曾想象她是一位脸颊红润、身上有纹身的离家出走者。她是一位诗人。她是一位宇航员。她是一位外科医生。她是一位舞者。她是一位创作者。她是一位母亲。

&emsp;&emsp;“乔迪，‘罗莎琳’这个名字有什么意义？”

&emsp;&emsp;“罗莎琳是威廉·莎士比亚[^11]的《罗密欧与朱丽叶》中的一位次要角色。这是罗莎琳德这个名字的变体，意为‘温顺的马’。”

&emsp;&emsp;一如既往，乔迪浇灭了我那天马行空的想象。

&emsp;&emsp;乔迪最终并未如研究所所期望的那样，成为首屈一指的家庭教育助手。九岁那年，莎伦向我介绍了普里娅和本，他们后来成了我所有科学、技术、工程和数学相关科目的私人导师。除此之外，没有其他科目。

&emsp;&emsp;普里娅和本每周会带着他们的笔记本电脑来三次，偶尔还会带上教科书（普里娅坚持说这是为了打好基础）。三角学和微积分由普里娅负责，她就像一座方程与变量的喷泉。本则负责生物学和化学。直到我证明了自己的数学能力后，普里娅才允许我学习物理。他们两人就像两块磁铁，总是互相调侃、互相打趣。但他们教会了我对数与算法、力学与动力学、有丝分裂与共生。

&emsp;&emsp;十四岁那年，我已经达到了私立中学的毕业要求。为了庆祝这一成就，普里娅、本和我挤进普里娅的那辆微型电动车，开车去了“乳品皇后”冰淇淋店。

&emsp;&emsp;当我们回到公寓时，莎伦打开门，冲我笑了笑。“现在我们可以开始你的真正教育了！”

&emsp;&emsp;“什么意思？” 

&emsp;&emsp;“你要去鲍威尔研究所工作了。和我一起。”

&emsp;&emsp;“和我们一起，”普里娅补充道。

&emsp;&emsp;莎伦瞥了普里娅一眼，又看了看本。

&emsp;&emsp;“你的胡子上沾了冰淇淋。”

&emsp;&emsp; 说完，她便消失在公寓昏暗的玄关里。

*未完待续……*

---

“计算机依然像以前一样具有压迫性，只是体积更小、价格更低、普及更广。如今，你甚至可以在客厅里受到计算机的压迫。” 

——泰德·尼尔森[^12]， 《计算机自由》

[^01]: **穿孔卡片** 也称打孔卡，是一块纸板，在预先知道的位置利用打洞与不打洞来表示数字消息。
[^02]: **宏大设计** 指既定命运。存在自由意志，但无法改变结果。
[^03]: **赛博格** 指有机体与生物机电一体化的生物，又称“人机融合”。
[^04]: **撒旦** 西方宗教中的堕落天使，统治地狱和众多魔鬼。
[^05]: **与我毫无关系** 图中高速公路路牌显示的即为本文作者名字（ISLAND MIKEY）。
[^06]: **另一个首字母缩写词** “Piece of Shit”（垃圾、废物、烂货，粗俗俚语，骂人 / 骂东西）的首字母缩写词也为“POS”。
[^07]: **人畜** 此处指人被当作牲畜一样加以剥削和饲养。
[^08]: **吃垃圾桶里东西** 请勿在现实生活中尝试（一位男性食用了从垃圾堆里捡来的甜甜圈，这是他的胃发生的变化）。
[^09]: **试管婴儿** 全称体外受精-胚胎移植。是一种辅助生殖技术，将女性卵子与男性精子取出，在实验室内体外受精并培养为胚胎，再植入子宫。
[^10]: 罗莎琳（Rosaline）首字母为 **R**。
[^11]: **威廉·莎士比亚**（William Shakespeare，1564-1616）英国戏剧家与诗人，被认为是西方文学史上最杰出卓越的文学家之一。
[^12]: **泰德·尼尔森**（Theodor Holm Nelson，1937- ）美国信息技术先驱，哲学家和社会学家。
